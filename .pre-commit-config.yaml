repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check --force-exclude .
        language: system
        always_run: true
        pass_filenames: false