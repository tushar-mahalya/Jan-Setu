# Base URL for the Jan-Setu API. Leave unset to use the Vite dev proxy
# (same-origin requests to /api and /auth are proxied to http://localhost:8000).
# Set this only when the frontend is served from a different origin than the API.
VITE_API_BASE=
